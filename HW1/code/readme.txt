1、deubg为主运行程序，x0为输入的初始值，本程序根据初始值来判断函数的维度。如 x0=[2 ,2 ,2 ,2]则默认为4个维度。该主程序的初始维度为100维度。
2、visualization为可视化函数，可生成二维和三维的迭代过程的图。其中，可视化函数默认为2个维度，只包含两个变量。
3、objectiveFunction为目标函数，根据初始值来判断函数的维度。
4、objectiveFunctionGradient为目标函数的梯度函数。
5、steepestDescent为梯度下降函数，迭代的终止条件为判断梯度的模长。